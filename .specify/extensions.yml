installed:
- ringer-delivery
settings:
  auto_execute_hooks: true
hooks:
  after_analyze:
  - extension: ringer-delivery
    command: speckit.ringer-delivery.prepare
    enabled: true
    optional: false
    priority: 10
    prompt: Execute speckit.ringer-delivery.prepare?
    description: Classify analyzed work and prepare bounded delegation before implementation
    condition: null
